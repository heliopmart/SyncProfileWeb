export default interface HeaderProject{
    header: {
        head: {
            buttonTitle: string,
            defaultTitle: string,
            defaultSubText: string,
            selectTittle: string
        },
        curiosity: {
            createDefaultString: string[],
            typeProject: string[]
        }
    }
}
